[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "traceproof"
version = "0.1.0"
description = "Capture, replay, and diff failed AI interactions."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
